# Recording 2026-08-25 6:42 PM — requirement list (verbatim-sourced)

Source: `~/Screenshots/Screen Recording 2026-08-25 at 6.42.34 PM.mov` (6:09).
Transcribed with the GPT API. Scope is CLOSED: only what is below.

---

## R1 — Copilot/chat mode never tells you the session needs a login  [0:00–1:20]
> "here it is showing previous replies. But if I turn it to terminal mode, it will
> show the actual state because I need to log in. But it should actually show the
> same situation here or some message like that… now if I send it, nothing will
> happen because the terminal is not ready for it. So it should be clearly stating
> something or saying something here to log in or stuff… in chat mode I don't know
> anything. First of all, this is the biggest problem."

Chat mode shows a stale transcript and an enabled composer while the far session is
sitting on a login/auth prompt. Sending does nothing, silently.
**Wanted:** chat mode surfaces the same live state the terminal does — a clear
message in the chat that the session is waiting on a login / is not ready for a
plain message, with the way through (switch to terminal). The composer must not
pretend it will work.

## R2 — Copilot chat header: back separate, the other two top-right  [1:20–1:47]
> "on this chat mode, switch this button and these three dots — I ask you to keep
> these two in the right side, on top right corner, not in one pill with the go
> back. So this go-back pill should be separate and the other two should be on
> right side exactly like this page. This terminal homepage."

**Wanted:** back button alone on the leading edge; mode-switch + `…` together in a
pill on the **trailing** edge, matching the Terminal home page's `[+ ⋯]` pill.

## R3 — Terminal session header: same move  [1:47–1:57], repeated [4:51–5:10]
> "but also in the terminal page, if we go inside a session, they are in left side.
> These two should be three dot and messaging button should be on right side here
> too."
> "then inside the terminal also, if you see three dots and this switch is on left
> side, it should be on right also."

**Wanted:** inside a terminal session, the chat-toggle + `…` pill moves to the
**trailing** edge. Back stays alone on the leading edge.

## R4 — Session title stays centred and readable  [1:57–2:18]
> "maybe the name of the session can come a little bit down so it can be readable…
> it will be folded with three dots, but it should stay centralized. It should not
> move according to the buttons top of it."

**Wanted:** the title is centred on the screen and does not shift because of what is
beside it. Truncating with `…` is fine. Give it a little more room/breathing space.

## R5 — Kill the expand button on the browser card (the black page)  [2:18–2:35]
> "this button is like not working the way I was expecting. This is something else.
> We don't need actually this part. We don't need this to be coming down like with
> black page."

The expand/`.full` verb (`arrow.up.left.and.arrow.down.right`) leaves a large black
area under the page. **Wanted:** remove that verb. Pane is either shown or folded.

## R6 — No separate keyboard button in the browser window  [2:35–3:26]
> "this keyboard should not be working like this. If we just click inside and type
> from our keyboard, it should work… I should not have to have this separate button
> of keyboard. It should just come up from down, and the original native button
> should be there to move it down if I want, not a separate keyboard here inside
> the browser window."

**Wanted:** tapping inside the page raises the system keyboard directly; the native
dismiss affordance puts it away. Remove the keyboard verb from the browser window
chrome (session card and the full window page bar).

## R7 — Keep only the chevron fold, no black area  [3:26–3:54]
> "this is good, but if I click on back it will not come up. It will come with this.
> Only this drop-down is required, like which can bring it to this state with the
> back panel, but this black area thing is not required. So just keep it simple."

**Wanted:** the chevron collapse/expand stays (header-only bar ⇄ page). Nothing else.
No black filler.

## R8 — The Browser tab must be purely browser  [3:54–4:51]
> "if we go to browser and if we go back, it is giving like this now. See, inside, it
> is taking me to directly terminal. So this page should be purely for only browser,
> not for terminal too. Terminal is only here, and only terminal is giving the
> browser window too. But browser side, it should not give the terminal window too…
> If I click here, there is again another button to take me to the same browser back.
> Then I go back again into the terminal page, which is too complicated. It should be
> just when I come to this browser page, here I should be able to see all the browsing
> windows. That's all, very simple."

**Wanted:** one-way. Terminal → browser is fine. Browser → terminal is not. Remove the
"B1 in that session · open it" session row and any other route from the Browser tab
into a terminal session. The Browser tab lists browser windows and opens browser
windows, nothing else.

## R9 — Artifacts must not show MD files  [5:26–5:45]
> "an artifact is still showing the MD files, which is — multiple times I have
> discussed about it. Artifact should not show the MD files. It should be only for
> purely the prototypes."

**Wanted:** the Artifacts panel stops listing markdown/prose files. Artifacts are
prototypes (and the visual artifacts he asked for originally), not a file browser —
Files is the file browser.
