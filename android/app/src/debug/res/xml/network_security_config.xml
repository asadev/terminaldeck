<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only.

  The development relay runs on the machine the emulator is on, with no certificate, so the debug
  build has to be allowed to open `ws://`. It is allowed for exactly two names: `10.0.2.2`, which is
  how an AVD reaches its host, and localhost. Everything else — including anything on the real
  internet — still requires TLS, and the release build has no exception at all.

  What makes this defensible rather than a hole is that the payload does not depend on it: every
  byte inside the socket is sealed by `crypto/Sealed.kt` before it is written, so cleartext here
  costs the metadata TLS would have hidden from a machine that is already this device's host.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
